import 'package:flutter/material.dart';

class NutritionPageHome extends StatelessWidget {
  const NutritionPageHome({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Nutrition page"),
    );
  }
}
