import 'package:flutter/material.dart';

class ProPageHome extends StatelessWidget {
  const ProPageHome({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Pro page home"),
    );
  }
}
